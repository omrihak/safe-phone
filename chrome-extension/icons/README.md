# Icons

This folder is referenced from `manifest.json` only when a future change starts
publishing the extension. For the internal builds we ship without an `"icons"`
manifest key (Chrome falls back to its default puzzle-piece icon), so this
folder can stay empty until we have branded artwork.
