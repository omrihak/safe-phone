{
  "name": "safephone-chrome-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Manifest V3 extension that consumes the SafePhone policy gist and enforces domain blocks in the browser.",
  "type": "module",
  "scripts": {
    "build:config": "node scripts/build-config.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "package": "node scripts/package.js"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}
